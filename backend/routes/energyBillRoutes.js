const express = require('express');
const router = express.Router();
// const energyBillController = require('../controllers/energyBillController');

// router.get('/users/:userId/bills', energyBillController.getBillsByUser);

// router.post('/bills', energyBillController.createBill);

module.exports = router;
